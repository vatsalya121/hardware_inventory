name: hardapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  pdf: ^3.8.4
  printing: ^5.2.0         # Downgraded for compatibility
  esc_pos_printer: ^4.1.0
  provider: ^6.0.0
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.0.0
  sqflite: ^2.0.0
  webview_flutter: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
